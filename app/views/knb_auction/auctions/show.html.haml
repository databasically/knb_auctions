= render :partial => "knb_auction/application/auction_engine"

.status-label
  - status_label(@auction.status) {@auction.status.to_s.upcase}
%h1.title
  = "Auction for"
  = link_to @auction.product_name, @auction.product
.time-left
  = auction_time_left( @auction ) + " left to bid"
-# .product-image{style: "width: 100px; height:100px;"}
-# .start_at="Start at: #{@auction.start_at.localtime.strftime("%m/%d/%y %I:%M:%S %p %Z")}"
-# .end_at="End at: #{@auction.end_at.localtime.strftime("%m/%d/%y %I:%M:%S %p %Z")}"
%br
%br

=render :partial => "bid"

%br
%br
%br

.actions
  = link_to 'Go back to Auctions', auctions_path
  - if current_user.admin?
    \|
    = link_to 'Edit this Auction', edit_auction_path(@auction)